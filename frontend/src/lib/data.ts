
// This file is deprecated and its contents have been moved to /lib/database.ts and the /data folder.
// It is kept to avoid breaking existing imports, but it should not be used for new development.

export type Chat = any;
export type Message = any;

export function getChats() {
    return [];
}

export function getChat(id: string) {
    return undefined;
}
